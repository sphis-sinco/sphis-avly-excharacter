function onAdded()
{
        var paths = [];
        var addScriptPath = function(path='') return paths.push('game/mods/ex-character/scripts/$path');

        addScriptPath('characterSelect/thirdCharacter.hxc');

        ScriptManager.loadScriptsByPaths(paths)
}